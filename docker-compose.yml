version: "3.8"

services:
  mtv_dl_web:
    build: .
    container_name: mtv_dl_web
    ports:
      - "8000:8000"
    volumes:
      - ./data:/data
      - ./downloads:/downloads
      - ./config:/config
      - ./.mtv_dl_web:/home/appuser/.mtv_dl_web
    environment:
      - PYTHONPATH=/app/src
      - PORT=8000
      - HOST=0.0.0.0
      - DATABASE_PATH=~/.mtv_dl_web/filmliste.sqlite
      - DOWNLOAD_QUALITY=best
      - TARGET_DIRECTORY=~/Downloads/mtv_dl
      - ENABLE_SUBTITLES=true
      - ENABLE_NFO=true
      - ENABLE_MKV_MERGE=false
    restart: unless-stopped
